#version 330

layout (location=0) in vec3 position;
layout (location=1) in float y;

uniform mat4 proj;
uniform mat4 view;
uniform mat4 model;

void main()
{
	vec3 positionWithY = vec3(position.x, y, position.z);
	vec4 vertex4 = (proj * view * model) * vec4(positionWithY, 1.0);
    gl_Position = vertex4;
    //normal = normalize(modelView * vec4(inNormal, 0.0)).xyz;
}