#version 330

in vec3 normal;

out vec4 fragColor;

void main()
{
	//fragColor = vec4(normal.x, normal.y, normal.z, 1.0);
	float light = max(dot(normal, vec3(0, 1, 0)), 0.0);
	vec3 base = vec3(0.7, 1.0, 0.5);
	fragColor = vec4(base * light, 1.0);
}